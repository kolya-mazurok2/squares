import styled from 'styled-components';

const StyledApp = styled.div`
  & > div,
  .board {
    margin-top: 20px;
  }

  .cell-position-list {
    margin-top: 26px;
  }
`;

export default StyledApp;
